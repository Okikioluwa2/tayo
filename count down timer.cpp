#include<stdio.h>
#include<time.h>
int main() {
struct timeval start,end;
struct tm *stop_time;
start.tv_sec=0;
start.tv_usec=0;
printf("Tap to start timer")

printf("The stop watch has started");
gettimeofday(&start,Null);
while (1) {
printf("The stopwatch is still running");	
getchar	
} 










}
